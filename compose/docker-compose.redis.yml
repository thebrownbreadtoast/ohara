version: "3.2"
services:
  redis:
    image: "redis:8.2.1"
    container_name: redis-server
    command: redis-server
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/var/lib/redis

volumes:
  redis-data:
